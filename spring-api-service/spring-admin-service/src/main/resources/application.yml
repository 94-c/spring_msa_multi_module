spring:
  application:
    name: spring-admin-service

  datasource:
    url: jdbc:mysql://localhost:3307/msa_project
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate.format_sql: true


server:
  port: 8084
